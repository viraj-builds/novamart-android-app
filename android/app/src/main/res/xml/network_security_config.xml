<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
        Allow cleartext traffic only for local debug (ADB) connections.
        All production traffic to CleverTap must go over HTTPS.
    -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user"/>
            <certificates src="system"/>
        </trust-anchors>
    </debug-overrides>

    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <!--
        Explicitly pin CleverTap EU endpoint and allow TLS 1.2 + 1.3.
        This resolves Vivo Funtouch OS TLS negotiation failures where
        the OS incorrectly drops TLS 1.3 connections to AWS CloudFront IPs.
    -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">clevertap-prod.com</domain>
        <domain includeSubdomains="true">clevertap.com</domain>
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </domain-config>
</network-security-config>
